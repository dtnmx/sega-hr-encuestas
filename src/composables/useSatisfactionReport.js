// Cálculos MEMECS sobre los filtros activos (periodo + ubicación).
// MVP: calcular en el frontend con un .select() simple (ver .md §8C);
// migrar a la función Postgres get_satisfaction_report si crece el dataset.
// Usa lib/memecs.js. Fase 2.

export function useSatisfactionReport() {
  throw new Error('useSatisfactionReport: pendiente Fase 2')
}
